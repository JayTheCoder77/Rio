[project]
name = "rio-core"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pydantic>=2",
    "unidiff>=0.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["rio_core"]