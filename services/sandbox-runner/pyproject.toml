[project]
name = "sandbox-runner"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "rio-review-core",
    "fastapi[standard]",
    "uvicorn[standard]",
]

[tool.uv.sources]
rio-review-core = { workspace = true }

[tool.fastapi]
entrypoint = "app.main:app"
port=8001