[project]
name = "sandbox-runner"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "rio-core",
    "fastapi[standard]",
    "uvicorn[standard]",
]

[tool.uv.sources]
rio-core = { workspace = true }