[project]
name = "cli"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "rio-core",
    "httpx>=0.28.1",
    "rich>=15.0.0",
    "typer>=0.27.1",
]

[project.scripts]
rio = "rio_cli.main:app"

[tool.uv.sources]
rio-core = { workspace = true }

[tool.uv]
package = true
